# Platform Manifests

The platform layer is intentionally small at first.

Current scope:
- namespace.yaml — DreamOps namespace
- test-app/ — lightweight NGINX workload used to validate Kubernetes networking

Future layers:
- ingress
- observability
- GitOps
- DreamOps agent
- trajectory/replay storage
