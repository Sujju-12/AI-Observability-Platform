apiVersion: v1
kind: Service
metadata:
  name: test-app
  namespace: dreamops
spec:
  selector:
    app.kubernetes.io/name: test-app
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
